import React from "react";

const helpSection = (title, items) => (
  <div style={{ marginBottom: 18 }}>
    <b style={{ display: "block", marginBottom: 8, color: "#38bdf8" }}>{title}</b>
    <ul style={{ margin: 0, paddingRight: 18, lineHeight: 1.85 }}>
      {items.map((item, i) => (
        <li key={i} style={{ marginBottom: 6 }}>
          {item}
        </li>
      ))}
    </ul>
  </div>
);

const code = (s) => (
  <code style={{ direction: "ltr", unicodeBidi: "embed", background: "#1e293b", padding: "1px 6px", borderRadius: 4 }}>{s}</code>
);

/**
 * راهنمای کامل فرم «مدیریت API هوش مصنوعی» برای ادمینی که پیش‌زمینه فنی ندارد.
 */
export const AI_API_FORM_HELP = () => (
  <div style={{ fontSize: 13, lineHeight: 1.85, textAlign: "right" }}>
    <p style={{ marginTop: 0, opacity: 0.95 }}>
      این صفحه فقط برای <b>ادمین سیستم</b> است. اینجا مشخص می‌کنید «برنامه از کدام سرویس هوش مصنوعی» و «با چه کلیدی»
      برای هر <b>کاربرد</b> (مثلاً خلاصه مدیریتی) درخواست بزند. کاربر عادی این صفحه را نمی‌بیند؛ فقط دکمه‌هایی مثل
      «تولید پیش‌نویس» در فرم‌ها می‌بیند و نتیجه را می‌گیرد.
    </p>

    {helpSection("نوع ارائه‌دهنده از کجا پر می‌شود؟", [
      <>
        لیست کشویی از سرور با درخواست {code("GET /api/admin/ai-provider-templates")} پر می‌شود؛ ریشهٔ آن جدول دیتابیس{" "}
        {code("tbl_ai_provider_templates")} است (مهاجرت {code("005_ai_provider_templates.sql")} چند قالب اولیه مثل{" "}
        {code("google_gemini")}، {code("openai_chat")} و {code("avalai")} را می‌سازد).
      </>,
      "هر ردیف قالب یک slug انگلیسی دارد؛ همان مقدار در فیلد «نوع ارائه‌دهنده» در ردیف API ذخیره می‌شود. سرور با همان slug قالب را پیدا می‌کند و موتور واقعی (مثلاً Gemini یا چت OpenAI) و پیش‌فرض‌های مدل و base_url را اعمال می‌کند.",
      "برای افزودن برند جدید، یک ردیف API با slug جدید بسازید و در JSON اضافی base_url را تنظیم کنید؛ در صورت نیاز از توسعه‌دهنده بخواهید قالب همان slug را در دیتابیس ثبت کند.",
    ])}

    {helpSection("کاربرد (usage_key) یعنی چه؟ مثلاً field.management_summary", [
      <>
        یک برچسب انگلیسی ثابت است که در کد برنامه تعریف شده تا بگوید این ردیف برای <b>کدام قابلیت</b> استفاده می‌شود.
      </>,
      <>
        مقدار {code("field.management_summary")} یعنی: «پیکربندی‌هایی که این کاربرد را دارند، برای{" "}
        <b>خلاصه مدیریتی گزارشات میدانی</b> به کار می‌روند».
      </>,
      "می‌توانید چند ردیف با یکسان بودن کاربرد ولی ترتیب (sort_order) متفاوت بسازید: اگر مدل اول خطا بدهد، برنامه سراغ ردیف بعدی می‌رود (زنجیره پشتیبان).",
      "برای قابلیت‌های جدید، توسعه‌دهنده یک کاربرد جدید در کد تعریف می‌کند؛ شما در این فرم ردیفی با همان نام می‌سازید.",
    ])}

    {helpSection("دکمه «بروزرسانی لیست از سرور» چه می‌کند؟", [
      "جدول را دوباره از سرور و دیتابیس می‌خواند — مثل رفرش صفحه، ولی فقط برای این لیست.",
      "اگر روی سرور یا تب دیگری ردیفی اضافه یا حذف شده، یا بعد از ذخیره می‌خواهید مطمئن شوید همه چیز به‌روز است، این دکمه را بزنید.",
      "«اعمال فیلتر» همان درخواست را با فیلترهای بالای جدول می‌فرستد؛ «پاک کردن فیلتر» فیلترها را خالی کرده و همه ردیف‌ها را نشان می‌دهد.",
    ])}

    {helpSection("ردیف جدید — قدم‌به‌قدم", [
      "۱) کاربرد: برای خلاصه میدانی معمولاً همان field.management_summary را نگه دارید مگر توسعه‌دهنده چیز دیگری گفته باشد.",
      "۲) ترتیب: عدد کوچکتر = اولویت بالاتر در همان کاربرد. برای پشتیبان دوم، همان کاربرد با ترتیب ۱.",
      "۳) عنوان فارسی: فقط برای تشخیص در جدول؛ روی منطق برنامه اثر ندارد.",
      "۴) نوع ارائه‌دهنده: slug از قالب‌های دیتابیس (مثلاً avalai برای AvalAI).",
      "۵) مدل: نام فنی مدل (مثلاً gemini-1.5-flash یا gpt-4o-mini).",
      "۶) تنظیمات اضافی (JSON): دما، حداکثر توکن، base_url — بدون بخش مانده اعتبار (مانده در بخش جداگانه پایین است).",
      "۷) بخش «مانده اعتبار حساب»: در صورت نیاز تیک بزنید و URL را از مستندات سرویس وارد کنید (راهنمای کامل در همان بخش).",
      "۸) اعتبارنامه: معمولاً متغیر محیطی — کلید را در .env سرور می‌گذارید و اینجا فقط نام متغیر را می‌نویسید.",
      "۹) ذخیره؛ با آزمایش (فلاسک) اتصال را تست کنید؛ با کیف پول مانده را ببینید.",
    ])}

    {helpSection("مانده اعتبار حساب — راهبر خودش تنظیم می‌کند (بدون SQL و بدون تغییر کد)", [
      "در فرم ویرایش/ایجاد ردیف API، بخش «مانده اعتبار حساب» را باز کنید و تیک بزنید.",
      "آدرس API مانده را از مستندات همان سرویس کپی کنید (مثلاً AvalAI: GET https://api.avalai.ir/user/v1/credit).",
      "فیلد «نام فیلد مانده در پاسخ JSON» همان کلیدی است که در پاسخ JSON مقدار باقی‌مانده را دارد — برای AvalAI معمولاً remaining_unit است؛ برای مانده ریالی remaining_irt را در فیلد ثانویه بگذارید.",
      "برچسب واحد (مثلاً UNIT یا USD) فقط برای نمایش در جدول است.",
      "بعد از ذخیره، در جدول دکمه کیف پول ظاهر می‌شود؛ با همان کلید API این ردیف (env یا secret) مانده را از سرور می‌خواند — کلید در مرورگر لو نمی‌رود.",
      "اگر نوع ارائه‌دهنده از قبل در قالب دیتابیس مانده دارد (مثل avalai)، با انتخاب آن نوع فیلدها خودکار پر می‌شوند؛ می‌توانید برای این ردیف خاص عوض کنید.",
      "اگر سرویس API مانده ندارد، تیک را بردارید — دکمه کیف پول نمایش داده نمی‌شود.",
    ])}

    {helpSection("مثال AvalAI", [
      <>آدرس مانده: {code("https://api.avalai.ir/user/v1/credit")}</>,
      <>فیلد مانده: {code("remaining_unit")} — فیلد ثانویه: {code("remaining_irt")} — واحد: {code("UNIT")}</>,
      "کلید: متغیر AVALAI_API_KEY در .env سرور یا secret ذخیره‌شده در همین ردیف.",
      "مستندات: docs.avalai.ir — بخش User API / GET credit",
    ])}

    {helpSection("مثال سرویس فرضی دیگر", [
      "از پشتیبانی یا مستندات سرویس بپرسید: «آدرس REST برای مانده حساب با Bearer API key چیست؟»",
      "یک بار با Postman یا curl تست کنید؛ ببینید پاسخ JSON چه کلیدی مانده را برمی‌گرداند (مثلاً balance یا credits_remaining).",
      "همان URL و نام فیلد را در فرم این صفحه وارد کنید و ذخیره کنید.",
    ])}

    {helpSection("Google Gemini", [
      "مدل مثل: gemini-1.5-flash یا gemini-1.5-pro",
      "در JSON اضافی می‌توانید maxOutputTokens و temperature بگذارید.",
      "کلید را با متغیر محیطی مثل GEMINI_API_KEY روی سرور تنظیم کنید.",
      "API مانده عمومی در سطح کلید Gemini معمولاً در همین فرم پشتیبانی نمی‌شود؛ از کنسول Google Cloud صورتحساب را ببینید.",
    ])}

    {helpSection("چت سازگار با OpenAI", [
      "برای OpenAI رسمی، مدل مثل gpt-4o-mini و متغیر OPENAI_API_KEY.",
      <>
        در JSON می‌توانید بگذارید: {code('{"base_url":"https://api.openai.com/v1"}')} — اگر خالی باشد همین پیش‌فرض استفاده می‌شود.
      </>,
      <>
        یا آدرس کامل: {code('{"chat_completions_url":"https://.../v1/chat/completions"}')}
      </>,
      "سرویس‌های لوکال (مثل Ollama با آداپتور OpenAI) اگر همان endpoint و Bearer را بپذیرند، با قالب engine=openai_chat قابل استفاده‌اند.",
      "برای مانده: فقط اگر سرویس endpoint جدا دارد، در بخش مانده اعتبار همان ردیف تنظیم کنید.",
    ])}

    {helpSection("ادمین در مقابل کاربر عادی", [
      "ادمین: این صفحه + مدیریت پرامپت‌ها + (در صورت نیاز) اکشن‌های AI فرم‌ها را تنظیم می‌کند.",
      "کاربر مجاز: فقط فرم را پر می‌کند و دکمه تولید را می‌زند؛ API key و انتخاب سرویس را نمی‌بیند.",
    ])}

    {helpSection("امنیت", [
      "ترجیحاً از متغیر محیطی (env) استفاده کنید.",
      "ذخیره کلید در دیتابیس فقط در صورت نیاز و با آگاهی از ریسک.",
      "درخواست مانده اعتبار همیشه از سرور backend زده می‌شود، نه از مرورگر کاربر.",
    ])}
  </div>
);
